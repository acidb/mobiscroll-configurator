'use client'
import React from 'react'
import type { ViewDescriptionClientProps } from 'payload'

export default function PresetDescription(props: ViewDescriptionClientProps) {
    return <div>Preset description</div>
}