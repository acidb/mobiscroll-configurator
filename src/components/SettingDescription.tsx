'use client'
import React from 'react'
import type { ViewDescriptionClientProps } from 'payload'

export default function SettingDescription(props: ViewDescriptionClientProps) {
    return <div>Setting description</div>
}