'use client'
import React from 'react'
import type { ViewDescriptionClientProps } from 'payload'

export default function FrameworkDescription(props: ViewDescriptionClientProps) {
    return <div>Framework description</div>
}